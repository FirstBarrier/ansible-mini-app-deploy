name: Docker CI + Push (Deploy Ready)

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ 소스 코드 체크아웃
      - name: Checkout source
        uses: actions/checkout@v4

      # 2️⃣ Docker 이미지 빌드
      - name: Build Docker image
        run: docker build -t my-mini-app:latest .

      # 3️⃣ Docker Hub 로그인 & 이미지 푸시
      - name: Login & Push Docker image
        run: |
          echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
          docker tag my-mini-app:latest wndlswo/mini-app:latest
          docker push wndlswo/mini-app:latest

      # ===============================
      # ⬇⬇⬇ 여기부터는 "배포 키 있을 때만"
      # ===============================

      # 4️⃣ Ansible 설치
      #- name: Install Ansible
      #  if: ${{ secrets.ACTIONS_DEPLOY_KEY != '' }}
      #  run: |
      #    sudo apt update
      #    sudo apt install -y ansible

      # 5️⃣ Ansible 문법 검사
      #- name: Ansible syntax check
      #  if: ${{ secrets.ACTIONS_DEPLOY_KEY != '' }}
      #  run: ansible-playbook deploy_mini_app.yml --syntax-check

      # 6️⃣ SSH 키 세팅
      #- name: Set up SSH key
      #  if: ${{ secrets.ACTIONS_DEPLOY_KEY != '' }}
      #  uses: webfactory/ssh-agent@v0.9.0
      #  with:
      #    ssh-private-key: ${{ secrets.ACTIONS_DEPLOY_KEY }}

      # 7️⃣ 실제 배포
      #- name: Deploy via Ansible
      #  if: ${{ secrets.ACTIONS_DEPLOY_KEY != '' }}
      #  run: ansible-playbook -i hosts.ini deploy_mini_app.yml
      